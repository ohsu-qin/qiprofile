extends ../layout/partial

block billboard
  | Projects

block content
  ul.qi-main-list
    p(*ngIf="isEmpty")
      | No projects were found.
    li(*ngFor="let project of projects")
      qi-project-item([project]="project")
