extends ../layout/partial

block billboard
  | {{ name }} Collection

block content
  ul.qi-main-list
    li(*ngFor="let subject of subjects", (click)="visitSubject(subject)")
      | {{ subject.number }}
  //- qi-collection-correlations([subjects]="subjects")
