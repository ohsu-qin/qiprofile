button.btn.btn-default.btn-lg.qi-btn(
  type='button', [disabled]="!isEnabled", (click)="togglePlay()"
)
  .glyphicon.glyphicon-play(*ngIf="!isPlaying")
  .glyphicon.glyphicon-pause(*ngIf="isPlaying")
